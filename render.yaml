services:
  - type: web
    name: flask-tracking-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn tracking_server:app --bind 0.0.0.0:$PORT"